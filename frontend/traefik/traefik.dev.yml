api:
  dashboard: true
  insecure: true

entryPoints:
  web:
    address: ":15170"
    http:
      redirections:
        entryPoint:
          to: websecure
          scheme: https
  
  websecure:
    address: ":15173"
    http:
      tls:
        certResolver: le

providers:
  file:
    directory: "/etc/traefik/dynamic"
    watch: true

certificatesResolvers:
  le:
    acme:
      email: admin@example.com
      storage: /acme/acme.json
      httpChallenge:
        entryPoint: web

log:
  level: DEBUG
