services:
  celery-worker-autotest:
    image: 'localhost/localhost/agent-backend-dev:python-3.12-cuda13'

    environment:
      GPU_MODE: cuda13

    # devices:
    #   - nvidia.com/gpu=all

    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: 1
              capabilities: [gpu]
