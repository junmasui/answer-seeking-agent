version = 1

# Exclude generated artifacts from analysis.
# These should never be in the code repository but they
# could show up in a CI environment.

exclude_patterns = [
  "frontend/node_modules/**",
  "backend/.venv/**",
  "**/__pycache__/",
]

test_patterns = [
  "frontend/app/tests/**/*.{js,ts,vue}",
  "backend/tests/**/*.py",
  "**/test_*.py",
  "**/*.spec.{js,ts}"
]

# Backend Configuration (Python with ruff)

[[analyzers]]
name = "python"
enabled = true
dependency_file_paths = [
  "backend/pyproject.toml",
  "backend/uv.lock"
]

  [analyzers.meta]
  runtime_version = "3.x.x"
  max_line_length = 120

# Frontend Configuration (JavaScript/Vue3)

[[analyzers]]
name = "javascript"
enabled = true
dependency_file_paths = [
  "frontend/app/package.json",
  "frontend/app/package-lock.json"
]

  [analyzers.meta]
  plugins = ["vue"]
  environment = ["browser", "vitest"]


[[analyzers]]
name = "shell"
enabled = true