services:
  fastapi-dev-server:
    image: 'localhost/answers-backend:python-3.12-cuda12'

    environment:
      GPU_MODE: cuda12

    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: 1
              capabilities: [gpu]

  fastapi-integration-server:
    image: 'localhost/answers-backend:python-3.12-cuda12'

    environment:
      GPU_MODE: cuda12

    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: 1
              capabilities: [gpu]

  celery-worker:
    image: 'localhost/answers-backend:python-3.12-cuda12'

    environment:
      GPU_MODE: cuda12

    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: 1
              capabilities: [gpu]

  celery-integration-worker:
    image: 'localhost/answers-backend:python-3.12-cuda12'

    environment:
      GPU_MODE: cuda12

    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: 1
              capabilities: [gpu]

