models:
  - type: main
    engine: openai
    model: gpt-4.1-nano

rails:
  output:
    flows:
      # - self check hallucination
      - self check output
      # - check output sensitive data

tracing:
  enabled: true
  adapters:
    - name: OpenTelemetry
      service_name: "nemo_guardrails"
