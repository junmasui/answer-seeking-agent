name: presidio

services:
  presidio-analyzer:
    restart: unless-stopped

    image: mcr.microsoft.com/presidio-analyzer:2.2.359
    profiles: [
      presidio,
      telemetry
      # infrastructure,
      # all
    ]

    env_file:
      - opentelemetry-instrument.env
    ports:
      - '27600:3000'
    networks:
      - agent-poc
    environment:
      - NLP_CONF_FILE=/conf/transformers.yaml
      - RECOGNIZER_REGISTRY_CONF_FILE=/conf/custom_recognizers.yaml
    volumes:
      - ../../dist:/dist:ro
      - ./run_presidio_analyzer.sh:/run_presidio_analyzer.sh:ro
      - ./custom-docker-entrypoint.sh:/custom-docker-entrypoint.sh:ro
      - ./pyproject.toml://usr/bin/presidio-analyzer/pyproject.toml
      - ./conf:/conf:ro
      - ./healthcheck.py:/healthcheck.py:ro

    entrypoint: [ '/custom-docker-entrypoint.sh' ]
    command: ["bash", "/run_presidio_analyzer.sh"]

    healthcheck:
      test: ["CMD", "python3", "/healthcheck.py"]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 5s

  presidio-anonymizer:
    restart: unless-stopped

    image: mcr.microsoft.com/presidio-anonymizer:latest
    profiles: [
      presidio,
      # infrastructure,
      # all
    ]

    ports:
      - '27601:5001'
    networks:
      - agent-poc

    healthcheck:
      test: ["CMD", "python3", "/healthcheck.py"]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 5s
