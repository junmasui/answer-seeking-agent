# from dotenv import load_dotenv

# def configure_environ(base_env, secrets_env):
#     # interpolate=False to provent the dotenv from interpolating the values
#     # override=True allows .env file values to override existing environment variables
#     load_dotenv(dotenv_path=base_env, interpolate=False, override=True)
#     load_dotenv(dotenv_path=secrets_env, interpolate=False, override=True)

# #
# # Configure the environment variables during this module's initialization.
# #
# configure_environ(base_env='backend.env', secrets_env='backend.secrets.env')
