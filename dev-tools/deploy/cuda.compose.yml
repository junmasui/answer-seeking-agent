services:
  dev-tools:
    image: 'localhost/localhost/agent-dev-tools:python-3.12-cuda13'

    environment:
      GPU_MODE: cuda13

    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: 1
              capabilities: [gpu]
