# Allow OpenSearch to start as a standalone node without requiring any cluster discovery settings
discovery.type=single-node

OPENSEARCH_JAVA_OPTS="-Xms128m -Xmx512m" # Adjust based on available resources
